#include<stdio.h>
#include<conio.h>
#define pi 3.142
#define r
#define inp_r scanf("%f",&r)
#define vol(r) ((4/3.0)*pi*(r*r*r))
#define area(r) (4*pi*(r*r))
#define areaandvol_p printf("area is%f",area(r)) printf("vol is%f",vol(r))

void main()
{
clrscr();
areanandvol

getch();
}